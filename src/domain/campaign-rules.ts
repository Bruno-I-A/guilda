/**
 * Regras das Campanhas (Fase 5) compartilhadas entre telas e instanciação.
 */

/**
 * Prioridade das tarefas materializadas a partir de template: média (2).
 * Decisão da 5b — o XP estimado do template e a instanciação da 5c usam
 * o mesmo valor para nunca divergirem.
 */
export const MATERIALIZED_PRIORITY = 2;
